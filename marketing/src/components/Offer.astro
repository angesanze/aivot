---
import { offer } from "../data/offer";
import type { Lang } from "../lib/i18n";

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const o = offer[lang];
---

<section id="try" class="mx-auto max-w-6xl px-6 py-20 sm:py-24">
  <div
    class="relative overflow-hidden rounded-3xl border border-emerald-500/20 bg-gradient-to-b from-emerald-500/[0.08] to-zinc-900/40 px-6 py-14 text-center sm:px-12"
  >
    <div
      class="pointer-events-none absolute inset-x-0 top-0 h-40 bg-[radial-gradient(ellipse_50%_100%_at_50%_0%,rgb(16_185_129/0.25),transparent)]"
    >
    </div>

    <p
      class="inline-flex items-center gap-2 rounded-full border border-emerald-500/30 bg-emerald-500/10 px-3 py-1 text-xs font-medium text-emerald-300"
    >
      {o.eyebrow}
    </p>
    <h2 class="mx-auto mt-5 max-w-2xl text-3xl font-bold tracking-tight text-zinc-50 sm:text-4xl">
      {o.title}
    </h2>
    <p class="mx-auto mt-4 max-w-xl text-zinc-400">{o.subtitle}</p>

    <div class="mt-8 flex flex-wrap items-center justify-center gap-3">
      <a
        href={o.primaryCta.href}
        class="rounded-lg bg-emerald-600 px-7 py-3.5 font-semibold text-white shadow-[0_0_30px_-6px_rgb(16_185_129/0.7)] transition hover:bg-emerald-500"
      >
        {o.primaryCta.label}
      </a>
      <a
        href={o.secondaryCta.href}
        class="rounded-lg border border-white/10 bg-white/[0.03] px-7 py-3.5 font-medium text-zinc-200 transition hover:bg-white/[0.06]"
      >
        {o.secondaryCta.label}
      </a>
    </div>

    <ul class="mx-auto mt-10 grid max-w-2xl gap-x-8 gap-y-3 text-left sm:grid-cols-2">
      {
        o.includes.map((it) => (
          <li class="flex items-center gap-2.5 text-sm text-zinc-300">
            <svg class="h-4 w-4 flex-none text-emerald-400" viewBox="0 0 20 20" fill="currentColor">
              <path
                fill-rule="evenodd"
                d="M16.7 5.3a1 1 0 0 1 0 1.4l-7.5 7.5a1 1 0 0 1-1.4 0L3.3 9.7a1 1 0 1 1 1.4-1.4l3.1 3.1 6.8-6.8a1 1 0 0 1 1.4 0Z"
                clip-rule="evenodd"
              />
            </svg>
            <span>{it}</span>
          </li>
        ))
      }
    </ul>
  </div>
</section>
